/* 
Write a loop that makes seven calls to console.log to output the following
triangle:
*/

function draw(num) {
var hash="";
for(var i = 1; i <=num; i++ ) {
        for(j = 0; j<i;j++) {
             hash+="#";
         }  
            hash+="\n";
    }
    console.log(pound)
}
draw(7);
